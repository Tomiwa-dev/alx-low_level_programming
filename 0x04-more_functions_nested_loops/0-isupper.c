#include "main.h"
#include <stdio.h>

/**
 * _isupper - check for upper case letter
 * Description: a program that checks for uppercase character
 * @c : character to check the case
 * Return: Always 0.
 */

int _isupper(int c)
{
	return (c >= 65 && c <= 90);
}
