#include <stdio.h>
#include <stdlib.h>
/**
 * main - main block
 * Description:  a program that prints the alphabet in lowercase,
 * followed by a new line
 * Return: 0
 */
int main(void)
{
	char lower = 'a';

	while (lower < 'z')
	{
		if (lower != 'q' && lower != 'e')
		{
			putchar(c);
		}
		lower++;
	}
	putchar('\n');
	return (0);

}
