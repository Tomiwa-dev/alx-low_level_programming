#include <stdio.h>
#include <stdlib.h>
/**
 * main - main block
 * Description: prints lowercase alphabet in reverse
 * Return: 0
 */
int main(void)
{
	char alp = 'z';

	while (alp >= 'a')
	{
		putchar(alp);
		c--;
	}

	putchar('\n');
	return (0);
}
